name: 02 - Eventos
on: 
  push:
  pull_request:
  workflow_dispatch:
  schedule:
   - cron: '0 0 5 * *'

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: ShowTrigger
        run: |
          echo "Event name: ${{ github.event_name }}" 
