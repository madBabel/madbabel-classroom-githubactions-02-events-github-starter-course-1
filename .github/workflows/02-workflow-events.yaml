name: 02 - Eventos
on:
  schedule:
   - cron: "10 * * * *"
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:          
      - name: ShowTrigger
        run: |
          echo "Event name: ${{ github.event_name }}" 